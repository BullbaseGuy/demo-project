# Runbook: handle an incident

A real incident notification must provide the task, classification, fingerprint,
source run, failed steps, automatic actions already attempted, remaining budget,
minimum user action and HANDOFF path.

Never paste secrets, unbounded logs or private runtime identifiers into the Issue.
