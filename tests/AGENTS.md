# Test Rules

- Tests must be deterministic and offline.
- Cover fail-closed behavior and security boundaries.
- Do not embed real credentials or private endpoints.
- Prefer small fixtures under `tests/fixtures/devflow/`.
