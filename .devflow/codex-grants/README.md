# One-time Codex/agent grants

This directory is intentionally empty in the default scaffold.

A future model-bearing activation must be introduced by a separate reviewed
pull request, bind one task commit and one failure fingerprint, declare a
bounded file allowlist, use a short TTL and permit at most one call.

A grant becomes consumed once reserved. Workflow reruns must never recreate a
model session.
